# FastAPI核心
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0

# 数据库
aiomysql==0.2.0          # 异步MySQL驱动
pymysql==1.1.0           # 同步MySQL驱动（初期过渡用）
cryptography==41.0.7

# 安全
python-jose[cryptography]==3.3.0  # JWT
passlib[bcrypt]==1.7.4            # 密码哈希
python-multipart==0.0.6           # 表单数据

# 环境变量
python-dotenv==1.0.0

# CORS
# FastAPI内置，无需额外安装

# 日志
python-json-logger==2.0.7

# 开发工具
pytest==7.4.3
pytest-asyncio==0.21.1
httpx==0.25.2            # 异步HTTP客户端（测试用）
