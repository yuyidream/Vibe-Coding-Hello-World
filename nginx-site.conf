# Nginx网站配置文件
# 用于Hello World网站

server {
    # 监听80端口（HTTP）
    listen 80 default_server;
    listen [::]:80 default_server;
    
    # 网站根目录
    root /var/www/hello-world;
    
    # 默认首页文件
    index index.html index.htm;
    
    # 服务器名称（可以改为您的域名）
    server_name _;
    
    # 网站访问配置
    location / {
        # 尝试访问文件，如果不存在返回404
        try_files $uri $uri/ =404;
    }
    
    # 静态文件缓存配置（可选，提升性能）
    location ~* \.(css|js|jpg|jpeg|png|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 7d;
        add_header Cache-Control "public, immutable";
    }
    
    # 安全头部配置
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    
    # 隐藏Nginx版本号
    server_tokens off;
    
    # 日志文件路径
    access_log /var/log/nginx/hello-world-access.log;
    error_log /var/log/nginx/hello-world-error.log;
}

# HTTPS配置（可选，需要SSL证书）
# 取消下面的注释并配置SSL证书路径即可启用HTTPS
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     
#     root /var/www/hello-world;
#     index index.html;
#     
#     server_name your-domain.com;
#     
#     # SSL证书配置
#     ssl_certificate /path/to/your/certificate.crt;
#     ssl_certificate_key /path/to/your/private.key;
#     
#     # SSL安全配置
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#     
#     location / {
#         try_files $uri $uri/ =404;
#     }
#     
#     access_log /var/log/nginx/hello-world-ssl-access.log;
#     error_log /var/log/nginx/hello-world-ssl-error.log;
# }
